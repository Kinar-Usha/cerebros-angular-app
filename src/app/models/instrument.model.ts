export interface Instrument{    
    instrumentId: string,
    externalIdType: string,
    externalId: string,
    categoryId: string,
    instrumentDescription: string,
    minQuantity: number,
    maxQuantity: number
}